# Native version of the pi calculation
function calculatePi(rounds) {
    limit = 2 * rounds + 1;
    pi = 0;

    for (i = 1 - 2 * rounds; i <= limit; i += 4) {
        pi += 1.0 / i;
    }
    pi *= 4;
    return pi;
}

void function main() {
    sayHi();
}